#include <stdio.h>

/**
 * main - print a statement using printf function
 * Return: 0 on success when compiled properly, non zero otherwise
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
