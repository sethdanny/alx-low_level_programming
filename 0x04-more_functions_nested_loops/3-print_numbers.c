#include "main.h"

/**
  * print_numbers - computes numbers
  * Return: 0 Always (success)
  */
void print_numbers(void)
{
	int n;

	for (n = 0; n < 10; n++)
	{
		_putchar(n);
	}
	_putchar('\n');

	return (0);
}
